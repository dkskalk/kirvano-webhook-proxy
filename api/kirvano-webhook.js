const APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbxdBiAuRYT2yqiP000I2893Vpz76tfy6_t63Xn8YkBMTYxxg2Jj90DaFOvTH7ATZCo_/exec"; const TOKEN_SECRETO = "Dou2023@"; export default async function handler(req, res) { if (req.method === "GET") { return res.status(200).json({ ok: true, mensagem: "Proxy Kirvano -> Apps Script ativo" }); } if (req.method !== "POST") { return res.status(405).json({ ok: false, erro: "método não permitido" }); } try { const corpoOriginal = req.body || {}; const corpoComToken = { ...corpoOriginal, token: TOKEN_SECRETO }; const resposta = await fetch(APPS_SCRIPT_URL, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(corpoComToken), redirect: "follow" }); const texto = await resposta.text(); let corpoResposta; try { corpoResposta = JSON.parse(texto); } catch { corpoResposta = { ok: true, bruto: texto }; } return res.status(200).json(corpoResposta); } catch (erro) { return res.status(200).json({ ok: false, erro: String(erro) }); } }
